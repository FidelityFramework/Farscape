# Library Verification & clefpak Architecture

## Overview

The Fidelity Framework uses **source-based linking** — no binary assemblies. Libraries are Clef source packages consumed by Composer's dependency walker. This requires a verification mechanism to ensure libraries are error-free before consumption.

## Verification Pipeline

```
C Headers → Farscape → Generated Clef Source + fidproj
                              ↓
                        farscape verify (CCS type-check, all modules reachable)
                              ↓ (pass)
                        clefpak pack (future — signed, compressed source artifact)
                              ↓
                        Package registry / dependency resolution
```

### The Reachability Trap

CCS performs reachability analysis on the PSG. Diagnostics on nodes not reachable from the entry point are demoted from `Error` → `Info` severity (`effectiveSeverity` in `Diagnostics.fs`). This means:
- A library with type errors **appears clean** as a dependency (nothing `open`s its modules)
- Errors only surface when application code reaches for those bindings
- Library verification must bypass this demotion to catch errors early

> **Not implemented (verified 2026-08-03).** The CLI root command is `generate | pilot | project`; there is no `verify` subcommand. The description below is a design. See `docs/09_Library_Verification.md` and `docs/14_Binding_Generation_Gaps.md` §6.

### `farscape verify` Command

```
farscape verify --fidproj <path-to-fidproj>
```

Invokes CCS against all library sources. Reports diagnostics at **true** severity (not demoted). Exit codes: 0=pass, 1=type errors, 2=infrastructure error.

### Feedback Loop

Verification failures are bugs in Farscape's generation pipeline (TypeMapper, WrapperCodeGenerator, etc.), not in C headers. Each fix addresses a class of C API patterns, maturing the generator systematically.

## fidproj Dependency Resolution (Mar 2026)

**Dependencies point at fidproj FILES, not directories.**

```toml
[dependencies]
platform = { path = "../Fidelity.Platform/CPU/Linux/x86_64/Fidelity.Platform.fidproj" }
libc = { path = "../Fidelity.Platform/CPU/Linux/x86_64/Fidelity.Libc.fidproj" }
```

**Why not directory-based discovery?** A library directory contains non-library artifacts:
- Pilot TOML files (Farscape regeneration hooks)
- Developer-owned Layer 3 / Overlay code
- Multiple independent fidproj files for sub-libraries

Directory-based discovery sweeps all of these in. File-based targeting is explicit, deterministic, and leaves tooling artifacts undisturbed.

**Composition**: Farscape generates one fidproj per pilot TOML invocation. Library authors compose parent fidproj files. Each fidproj is independently addressable — an app needing only libc references `Fidelity.Libc.fidproj` directly.

### Open-Driven Source Resolution

The `open` declaration is the **demand signal** for source loading. Just because a fidproj lists source files doesn't mean they get loaded — only when the application `open`s their namespace.

- `open Fidelity.Image.Stb` → loads only Stb source files (not Resvg)
- `open Fidelity.Image` → loads ALL files under Fidelity.Image namespace
- No `open` = source files never loaded, even if fidproj is in dependency chain

**Namespace discovery**: Each `.clef` file declares its module on line 1 (`module Fidelity.Libc.IO`). A lightweight first-line scan maps namespace → file without full parsing.

**Design-time**: Overly broad `open` inflates PSG unnecessarily. Lattice (language server) should surface refinement suggestions — "pit of success" guidance toward namespace specificity.

Example:
```
Fidelity.Platform/CPU/Linux/x86_64/
  Fidelity.Platform.fidproj           ← hand-authored root (Types, Console, etc.)
  Fidelity.Libc.fidproj              ← generated by Farscape
  pilot/libc.pilot.toml              ← regeneration hook (not part of library)
  Bindings/Libc/...                  ← generated sources
```

## clefpak (Future)

Signed, compressed source artifact for the Fidelity package management system:
- Contains verified Clef source (not compiled binaries)
- Cryptographic signature for provenance
- Metadata: platform target, NTU version, dependency graph
- Verification is the prerequisite gate for packaging

## Farscape → Atelier Evolution

Farscape is an interim tool. Its capabilities evolve into Composer features accessible through Atelier IDE:

| Current (Farscape) | Future (Composer/Atelier) |
|---|---|
| C header parsing + binding generation | **Transpose**: Typed dynamic binding with full NTU/BAREWire integration |
| Layer 1 + Layer 2 + Overlay output | Integrated into Composer's binding pipeline |
| `farscape verify` *(planned)* | Composer library verification (built-in) |
| Pilot TOML project system | Composer project dependencies |

**Transcribe** (full algorithmic port from foreign source) extends beyond Farscape's scope — it handles source-to-source translation, not just FFI binding.

## Related Documentation

- Farscape: `docs/09_Library_Verification.md`
- Farscape: `docs/roadmap/99_composer-transcribe-vision.md`
- Composer: `docs/CCS_Architecture.md`
- Atelier: `docs/08_tooling_integration.md`
